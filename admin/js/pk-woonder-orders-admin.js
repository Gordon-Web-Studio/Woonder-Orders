(function( $ ) {
  'use strict';


  $(function() {
  // 	var wpColorPickerOptions = {
	 //    change: function(event, ui){
	 //    	// console.log($("#" + event.target.id).val());
	 //    	$('#custom-status-color-value').val($('#'+event.target.id).val());
	 //    },
		// };
  //   // Add Color Picker to all inputs that have 'color-field' class
  // 	$('.pk-color-field').wpColorPicker(wpColorPickerOptions);

  });

})( jQuery );
